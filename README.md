# throughput-calculator
System / Server throughput calculator

A lightweight, single-file calculator for visualising server and system throughput. Enter any one value — RPS, RPM, RPH, RPD, RPW, or RPY — and the rest update automatically.
Supports multi-instance scaling so you can compare per-node throughput against total system capacity side by side.

